# Enable the Prometheus endpoint
management.endpoints.web.exposure.include=health,info,prometheus

# Optionally, you can set the path for the Prometheus endpoint
management.endpoints.web.base-path=/actuator
